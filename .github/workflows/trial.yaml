name: First
on: [push]

jobs:
  run-commands:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "Hello World"
      - name: check versions
        run: |
          node -v
          npm -v
